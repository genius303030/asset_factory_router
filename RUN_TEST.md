# RUN_TEST.md

Run all unit tests to verify the integrity of the V0.2_DEV system:

```bash
cd D:\AI_LAB\employee_test\asset_factory_router
python -m unittest discover tests
```

Tests cover brief loading, schema validation, provider registry, configurable routing logic, prompt engine formatting, asset pack writing, and CLI smoke tests.
