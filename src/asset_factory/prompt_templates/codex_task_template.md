### Codex Implementation
- Use the generated assets in the designated `assets/` folder.
- Follow the {style} theme for CSS/Tailwind variables.
- Ensure the layout matches the {usage_context} requirements.
